function A = Inverse( A )
%INVERSE Summary of this function goes here
%   Detailed explanation goes here

for k = 1 : n
    A(k, k) = 1 / A(k, k);
    for j = [1:k-1 k+1:n]
        A(k, j) = A(k, j)



end

